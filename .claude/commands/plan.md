Use the Task tool to spawn an architect subagent to create an implementation plan:

$ARGUMENTS

Use subagent_type="architect" which runs on Opus for high-reasoning planning tasks. Pass the full user request as the prompt.
