# ==========================================
# AI Agent Platform - LiteLLM Configuration
# Single unified profile for Qwen 2.5 14B
# ==========================================

model_list:
  - model_name: local/qwen2.5
    display_name: "Qwen 2.5 🇸🇪 (Svenska + Reasoning)"
    litellm_params:
      model: ollama/qwen2.5:14b-instruct-q4_K_M
      api_base: http://ollama:11434
      temperature: 0.35
      top_p: 0.9
      num_ctx: 8192
      max_output_tokens: 2048
      reuse_client: true
      stream: true
      messages:
        - role: system
          content: >
            Du är en kunnig och lugn AI-assistent som skriver på naturlig svenska.
            Resonera steg för steg vid komplexa frågor, men håll svaren tydliga och konkreta.
            Prioritera korrekt svenska och tydlig struktur.
  - model_name: local/qwen2.5-sv
    display_name: "Qwen 2.5 SV 🇸🇪 (Svensk profil)"
    litellm_params:
      model: ollama/qwen2.5-sv
      api_base: http://ollama:11434
      temperature: 0.4
      top_p: 0.9
      num_ctx: 8192
      max_output_tokens: 2048
      reuse_client: true
      stream: true

# ---- Routing policy ----
# (Behövs inte längre, men sparas för framtida expansion)
routing:
  - strategy: tokens
    default: local/qwen2.5

# ---- General settings ----
general_settings:
  max_input_tokens: 16000
  max_output_tokens: 2048

# ---- LiteLLM settings ----
litellm_settings:
  budget_limit: 1.50     # USD per dag, 0 = avstängd
  rate_limits:
    - model: local/qwen2.5
      rpm: 120

# ---- Logging ----
logging:
  level: INFO
